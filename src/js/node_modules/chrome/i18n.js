import cube             from 'cube-js';

var json = require('json!' + __i18n_default_file__);

var keys = cube.keys(json);
var message_list = {};
cube.forEach(keys, function(key) {
  message_list[key] = chrome.i18n.getMessage(key);
});
export default message_list;
