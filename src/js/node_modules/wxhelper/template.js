var swig = require('swig');

module.exports = {
  renderHtml: function(templateKey, data) {
    var template = swig.compile(require('raw!' + __template_dir__ + '/' + templateKey + '.html'));
    return template(data);
  },
};
